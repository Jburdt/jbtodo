<nav class="flex justify-between px-20 py-10 items-center bg-white">
  <h1 class="text-xl text-gray-800 font-bold">JBTodo</h1>
  <div class="flex items-center">
    <ul class="flex items-center space-x-6">
      <% if user_signed_in? %>
        <li class="font-semibold text-gray-700"><%= link_to "Home", root_path %></li>
        <li class="font-semibold text-gray-700"><%= link_to "Todos", todos_path %></li>
        <li class="font-semibold text-gray-700"><%= link_to "Profile", edit_user_registration_path %></li>
        <li class="font-semibold text-gray-700"><%= button_to "Logout", destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li class="font-semibold text-gray-700"><%= link_to "Home", root_path %></li>
        <li class="font-semibold text-gray-700"><%= link_to "Login", new_user_session_path %></li>
        <li class="font-semibold text-gray-700"><%= link_to "Sign Up", new_user_registration_path %></li>
      <% end %>
    </ul>
  </div>
</nav>
